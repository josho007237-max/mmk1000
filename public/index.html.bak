<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>MMK1000 Panel</title>

  <!-- Fonts: Inter (body) + Space Grotesk (headings) -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Space+Grotesk:wght@600;700;800&display=swap" rel="stylesheet">

  <style>
    :root{
      /* Theme */
      --bg:#101112;
      --surface:#18181A;
      --border:#353535;
      --text:#FFFFFF;
      --muted:#9AA0A6;
      --accent:#E35449;
      --mint:#51BD89;
      --card:#F5F6F7;
      --cardText:#111318;

      /* Type */
      --font-head: "Space Grotesk", system-ui, -apple-system, Segoe UI, Arial, sans-serif;
      --font-body: "Inter", system-ui, -apple-system, Segoe UI, Arial, sans-serif;

      /* Layout */
      --padX:24px;
      --gap:12px;
      --radius:20px;
      --radius2:14px;

      /* Tap */
      --tapMin:44px;
      --tap:48px;
    }

    html{ font-size:16px; }
    body{
      font-family:var(--font-body);
      background:var(--bg);
      color:var(--text);
      margin:0 auto;
      max-width:1100px;
      padding:var(--padX);
      line-height:1.6;
    }

    /* Typography */
    h1,h2,h3{ font-family:var(--font-head); margin:0; }
    h1{
      font-size:clamp(40px,6vw,56px);
      font-weight:800;
      line-height:1.05;
      text-align:center;
      letter-spacing:-0.02em;
      margin:10px 0 16px;
    }
    h2{ font-size:clamp(22px,3.2vw,28px); font-weight:800; }
    h3{ font-size:clamp(16px,2.4vw,20px); font-weight:700; }

    .muted{ color:var(--muted); font-size:12px; letter-spacing:.02em; }
    .ok{ color:var(--mint); }
    .bad{ color:var(--accent); }

    /* Layout */
    .row{ display:flex; gap:var(--gap); flex-wrap:wrap; }
    .tabs{ display:flex; gap:8px; flex-wrap:wrap; justify-content:center; margin:10px 0 18px; }

    .card{
      background:var(--surface);
      border:1px solid var(--border);
      border-radius:var(--radius);
      padding:16px;
      flex:1;
      min-width:280px;
    }

    /* Optional: white card style (เผื่อพี่อยากทำ section ขาว) */
    .card-light{
      background:var(--card);
      color:var(--cardText);
      border-color:#E6E6E6;
    }

    /* Tabs */
    .tab-btn{
      border:1px solid var(--border);
      background:var(--surface);
      color:var(--text);
      border-radius:999px;
      padding:10px 14px;
      min-height:var(--tap);
      cursor:pointer;
      font-size:16px;
    }
    .tab-btn.active{
      border-color:var(--accent);
      box-shadow:0 0 0 2px rgba(227,84,73,.25);
    }

    /* Buttons */
    button{
      cursor:pointer;
      min-height:52px;
      min-width:var(--tapMin);
      padding:10px 14px;
      font-size:16px;
      border-radius:999px;
      border:1px solid var(--border);
      background:var(--surface);
      color:var(--text);
      transition:transform .08s ease, filter .12s ease, box-shadow .12s ease;
    }
    button:hover{ filter:brightness(1.06); }
    button:active{ transform:translateY(1px); }
    button:focus-visible{
      outline:none;
      box-shadow:0 0 0 2px rgba(227,84,73,.9);
    }

    /* Primary / Secondary / Card button spec */
    .btn-primary{
      background:var(--accent);
      border-color:transparent;
      min-height:56px;
      font-weight:600;
    }
    .btn-secondary{
      min-height:52px;
      position:relative;
      padding-right:52px;
    }
    .btn-secondary::after{
      content:"→";
      position:absolute;
      right:10px;
      top:50%;
      transform:translateY(-50%);
      width:28px; height:28px;
      border-radius:999px;
      border:1px solid var(--border);
      display:flex;
      align-items:center;
      justify-content:center;
      font-weight:700;
      color:var(--text);
      background:transparent;
    }
    .btn-card{
      background:#0F1114;
      border-color:#0F1114;
      border-radius:14px;
      min-height:52px;
      font-weight:600;
    }

    /* Inputs */
    input,select{
      min-height:var(--tap);
      padding:10px 12px;
      font-size:16px;
      border-radius:var(--radius2);
      border:1px solid var(--border);
      background:#0F1114;
      color:var(--text);
    }
    input::placeholder{ color:rgba(154,160,166,.7); }

    /* Sections */
    .section{ display:none; }
    .section.active{ display:block; }

    /* Tables */
    .table-wrap{ overflow-x:auto; -webkit-overflow-scrolling:touch; }
    table{ width:100%; border-collapse:collapse; }
    th,td{
      border-bottom:1px solid rgba(53,53,53,.7);
      padding:10px;
      font-size:14px;
      text-align:left;
      white-space:nowrap;
    }

    /* Banner */
    #statusBanner{
      padding:10px 12px;
      border-radius:14px;
      border:1px solid var(--border);
      margin:10px 0 14px;
      display:none;
      background:rgba(24,24,26,.65);
    }
    #statusBanner.ok{ border-color:rgba(81,189,137,.6); color:var(--mint); }
    #statusBanner.error{ border-color:rgba(227,84,73,.7); color:var(--accent); }
    #statusBanner.info{ border-color:rgba(154,160,166,.4); color:var(--muted); }

    .loading{ opacity:.65; pointer-events:none; }

    /* Queue cards on mobile */
    #qCards{ display:none; }
    .q-card{
      background:var(--surface);
      border:1px solid rgba(53,53,53,.6);
      border-radius:16px;
      padding:12px;
      margin-bottom:10px;
    }
    .q-card .row{ gap:6px; }

    /* ✅ TMN Real Config: desktop 2 col, mobile 1 col */
    .tmn-grid{
      display:grid;
      grid-template-columns:1fr 1fr;
      gap:10px;
      margin-top:10px;
    }
    .tmn-grid label{ display:block; }

    /* Mobile */
    @media (max-width: 720px){
      body{ padding:16px; }
      .row{ flex-direction:column; }
      .card{ min-width:auto; }
      #qTable{ display:none; }
      #qCards{ display:block; }

      .tmn-grid{ grid-template-columns:1fr; } /* ✅ แถวเดียวลงมา */
    }
  </style>
</head>

<body>
  <h1>MMK1000 Panel</h1>

  <div id="statusBanner" class="info"></div>

  <div class="tabs">
    <button class="tab-btn active" data-tab="dashboard">Dashboard</button>
    <button class="tab-btn" data-tab="withdraw">Withdraw</button>
    <button class="tab-btn" data-tab="qr">QR</button>
  </div>

  <div class="card">
    <div class="row">
      <div>
        <div class="muted">ADMIN KEY</div>
        <input id="adminKey" placeholder="mmk1000" style="width:220px" />
      </div>
      <div style="align-self:flex-end; display:flex; gap:8px; flex-wrap:wrap;">
        <button class="btn-primary" onclick="saveAdminKey()">Save</button>
        <button onclick="testAdmin()">Load</button>
        <button onclick="pingHealth()">Ping /api/health</button>
        <button onclick="authCheck()">Auth Check</button>
        <button id="debugBtn" onclick="toggleDebug()">DEBUG: OFF</button>
        <span id="status" class="muted"></span>
      </div>
    </div>
  </div>

  <div id="tab-dashboard" class="section active">
    <div class="row">
      <div class="card">
        <h3>1) Dashboard</h3>
        <div class="row" style="align-items:flex-end;">
          <div>
            <div class="muted">Start</div>
            <input id="start" type="date" />
          </div>
          <div>
            <div class="muted">End</div>
            <input id="end" type="date" />
          </div>
          <div>
            <button class="btn-primary" onclick="loadDashboard()">Refresh</button>
          </div>
        </div>

        <p style="margin:12px 0 10px;">Balance: <b id="balance">-</b> <span class="muted" id="balMode"></span></p>

        <div class="table-wrap">
          <table>
            <thead><tr><th>time</th><th>type</th><th>amount</th><th>id</th></tr></thead>
            <tbody id="txBody"></tbody>
          </table>
        </div>

        <div class="muted" style="margin-top:10px;">วันนี้โหมด mock จะเห็นรายการตัวอย่าง</div>
      </div>
    </div>
  </div>

  <div id="tab-withdraw" class="section">
    <div class="card">
      <h3>2) Withdraw (คิว + อนุมัติ)</h3>
      <div class="row" style="align-items:flex-end;">
        <div>
          <div class="muted">Type</div>
          <select id="wType" onchange="renderWithdrawFields()">
            <option value="bank">Bank</option>
            <option value="promptpay">PromptPay</option>
            <option value="wallet">TrueWallet (P2P)</option>
          </select>
        </div>
        <div>
          <div class="muted">Amount</div>
          <input id="wAmount" type="number" step="0.01" inputmode="decimal" />
        </div>

        <div style="flex:2" id="wFields"></div>

        <div style="display:flex; gap:8px; flex-wrap:wrap;">
          <button class="btn-primary" onclick="createWithdraw()">Add Queue</button>
          <button onclick="loadQueue()">Refresh Queue</button>
        </div>
      </div>

      <div id="qCards" style="margin-top:12px;"></div>

      <div class="table-wrap" id="qTable" style="margin-top:12px;">
        <table>
          <thead><tr><th>status</th><th>type</th><th>amount</th><th>dest</th><th>actions</th></tr></thead>
          <tbody id="qBody"></tbody>
        </table>
      </div>

      <div class="muted" style="margin-top:10px;">โหมด mock: กด Send จะ “จำลอง” ว่าส่งสำเร็จ</div>
    </div>
  </div>

  <div id="tab-qr" class="section">
    <div class="card">
      <h3>3) Upload QR ปลายทาง (PromptPay)</h3>
      <div class="row" style="align-items:flex-end;">
        <div style="flex:1">
          <div class="muted">QR Image</div>
          <input id="qrFile" type="file" accept="image/*" />
        </div>
        <div>
          <button class="btn-primary" onclick="decodeQr()">Decode</button>
        </div>
      </div>
      <pre id="qrOut" style="white-space:pre-wrap; margin-top:12px;"></pre>
    </div>
  </div>

  <!-- TMN Real Config -->
  <details style="margin:12px 0;">
    <summary><b>TMN Real Config (กรอกครั้งเดียว/เครื่องนี้)</b></summary>

    <!-- ✅ เปลี่ยนจาก inline grid เป็น class tmn-grid -->
    <div class="tmn-grid">
      <label>TMNONE_KEYID<br><input id="tmn_keyid" placeholder="ตัวเลข" /></label>
      <label>TMN_MSISDN<br><input id="tmn_msisdn" placeholder="668xxxxxxxxx" /></label>

      <label>TMN_LOGIN_TOKEN<br><input id="tmn_login_token" type="password" placeholder="token" /></label>
      <label>TMN_TMN_ID<br><input id="tmn_tmn_id" type="password" placeholder="tmn_id" /></label>

      <label>TMN_DEVICE_ID (optional)<br><input id="tmn_device_id" placeholder="" /></label>
      <label>TMN_PIN6<br><input id="tmn_pin6" type="password" placeholder="123456" /></label>

      <label>PROXY_IP (optional)<br><input id="tmn_proxy_ip" placeholder="" /></label>
      <label>PROXY_USERNAME (optional)<br><input id="tmn_proxy_user" placeholder="" /></label>

      <label>PROXY_PASSWORD (optional)<br><input id="tmn_proxy_pass" type="password" placeholder="" /></label>
    </div>

    <div style="margin-top:10px;display:flex;gap:8px;flex-wrap:wrap;">
      <button class="btn-primary" id="tmn_save" type="button">Save TMN</button>
      <button id="tmn_clear" type="button">Clear TMN</button>
    </div>
  </details>

  <script src="/app.js?v=20260209-2"></script>
</body>
</html>
