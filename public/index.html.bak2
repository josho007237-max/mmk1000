<!doctype html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>MMK1000 Panel</title>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&family=Space+Grotesk:wght@600;700;800&display=swap" rel="stylesheet">

  <style>
    :root{
      --bg:#101112;
      --surface:#18181A;
      --border:#353535;
      --text:#FFFFFF;
      --muted:#9AA0A6;
      --accent:#E35449;
      --mint:#51BD89;

      --font-head: "Space Grotesk", system-ui, -apple-system, Segoe UI, Arial, sans-serif;
      --font-body: "Inter", system-ui, -apple-system, Segoe UI, Arial, sans-serif;

      --padX:24px;
      --gap:12px;
      --r20:20px;
      --r14:14px;

      --tapMin:44px;
      --tap:48px;
    }

    html{font-size:16px}
    body{
      font-family:var(--font-body);
      background:var(--bg);
      color:var(--text);
      margin:0 auto;
      max-width:1100px;
      padding:var(--padX);
      line-height:1.6;
    }

    h1,h2,h3{font-family:var(--font-head);margin:0}
    h1{
      font-size:clamp(40px,6vw,56px);
      font-weight:800;
      line-height:1.05;
      text-align:center;
      letter-spacing:-0.02em;
      margin:10px 0 16px;
    }
    h3{font-size:clamp(16px,2.4vw,20px);font-weight:700}

    .muted{color:var(--muted);font-size:12px;letter-spacing:.02em}
    .row{display:flex;gap:var(--gap);flex-wrap:wrap}
    .card{
      background:var(--surface);
      border:1px solid var(--border);
      border-radius:var(--r20);
      padding:16px;
      flex:1;
      min-width:280px;
    }

    #statusBanner{
      padding:10px 12px;
      border-radius:14px;
      border:1px solid var(--border);
      margin:10px 0 14px;
      display:none;
      background:rgba(24,24,26,.65);
    }
    #statusBanner.ok{border-color:rgba(81,189,137,.6);color:var(--mint)}
    #statusBanner.error{border-color:rgba(227,84,73,.7);color:var(--accent)}
    #statusBanner.info{border-color:rgba(154,160,166,.4);color:var(--muted)}

    .tabs{display:flex;gap:8px;flex-wrap:wrap;justify-content:center}
    .tab-btn{
      border:1px solid var(--border);
      background:var(--surface);
      color:var(--text);
      border-radius:999px;
      padding:10px 14px;
      min-height:var(--tap);
      cursor:pointer;
      font-size:16px;
    }
    .tab-btn.active{
      border-color:var(--accent);
      box-shadow:0 0 0 2px rgba(227,84,73,.25);
    }

    input,select{
      min-height:var(--tap);
      padding:10px 12px;
      font-size:16px;
      border-radius:var(--r14);
      border:1px solid var(--border);
      background:#0F1114;
      color:var(--text);
    }
    input::placeholder{color:rgba(154,160,166,.7)}

    button{
      cursor:pointer;
      min-height:52px;
      min-width:var(--tapMin);
      padding:10px 14px;
      font-size:16px;
      border-radius:999px;
      border:1px solid var(--border);
      background:var(--surface);
      color:var(--text);
      transition:transform .08s ease, filter .12s ease, box-shadow .12s ease;
    }
    button:hover{filter:brightness(1.06)}
    button:active{transform:translateY(1px)}
    button:focus-visible{outline:none;box-shadow:0 0 0 2px rgba(227,84,73,.9)}
    button:disabled{opacity:.55;pointer-events:none}

    .btn-primary{
      background:var(--accent);
      border-color:transparent;
      min-height:56px;
      font-weight:600;
    }
    .btn-secondary{
      min-height:52px;
      position:relative;
      padding-right:52px;
    }
    .btn-secondary::after{
      content:"→";
      position:absolute;
      right:10px;top:50%;
      transform:translateY(-50%);
      width:28px;height:28px;
      border-radius:999px;
      border:1px solid var(--border);
      display:flex;align-items:center;justify-content:center;
      font-weight:700;
    }

    .btn-tmn.ok{
      background:rgba(81,189,137,.18);
      border-color:rgba(81,189,137,.65);
    }
    .btn-tmn.ok::before{content:"✓ ";font-weight:800;color:var(--mint)}
    .btn-approve{
      background:rgba(81,189,137,.18);
      border-color:rgba(81,189,137,.65);
    }
    .btn-send{
      background:var(--accent);
      border-color:transparent;
      font-weight:700;
    }

    .field-ok{border-color:rgba(81,189,137,.75)!important; box-shadow:0 0 0 2px rgba(81,189,137,.18)}
    .field-bad{border-color:rgba(227,84,73,.85)!important; box-shadow:0 0 0 2px rgba(227,84,73,.18)}

    .section{display:none}
    .section.active{display:block}

    .table-wrap{overflow-x:auto;-webkit-overflow-scrolling:touch}
    table{width:100%;border-collapse:collapse}
    th,td{border-bottom:1px solid rgba(53,53,53,.7);padding:10px;font-size:14px;text-align:left;white-space:nowrap}

    /* Mobile */
    #qCards{display:none}
    .q-card{border:1px solid rgba(53,53,53,.6);border-radius:16px;padding:12px;margin-bottom:10px;background:var(--surface)}
    @media (max-width:720px){
      body{padding:16px}
      .row{flex-direction:column}
      .card{min-width:auto}
      #qTable{display:none}
      #qCards{display:block}
    }

    .tmn-grid{
      display:grid;
      grid-template-columns:1fr 1fr;
      gap:10px;
      margin-top:10px;
    }
    @media (max-width:720px){
      .tmn-grid{grid-template-columns:1fr}
    }
  </style>
</head>

<body>
  <h1>MMK1000 Panel</h1>

  <div id="statusBanner" class="info"></div>

  <!-- ✅ ปุ่ม TMN ก่อน Dashboard -->
  <div class="row" style="align-items:center; justify-content:space-between; margin:10px 0 16px;">
    <button id="tmnToggleBtn" class="btn-secondary btn-tmn" type="button" onclick="toggleTmnPanel()">TMN Real Config</button>
    <div class="tabs">
      <button class="tab-btn active" data-tab="dashboard" type="button">Dashboard</button>
      <button class="tab-btn" data-tab="withdraw" type="button">Withdraw</button>
    </div>
  </div>

  <!-- ✅ TMN Panel (ซ่อน/แสดงด้วยปุ่ม) -->
  <div id="tmnPanel" class="card" style="display:none; margin-bottom:12px;">
    <h3>TMN Real Config</h3>
    <div class="muted">กรอกครั้งเดียว/เครื่องนี้ — กรอกครบแล้วปุ่มจะเป็นสีเขียว</div>

    <div class="tmn-grid">
      <label>TMNONE_KEYID<br><input id="tmn_keyid" placeholder="ตัวเลข" /></label>
      <label>TMN_MSISDN<br><input id="tmn_msisdn" placeholder="668xxxxxxxxx" /></label>

      <label>TMN_LOGIN_TOKEN<br><input id="tmn_login_token" type="password" placeholder="token" /></label>
      <label>TMN_TMN_ID<br><input id="tmn_tmn_id" type="password" placeholder="tmn_id" /></label>

      <label>TMN_DEVICE_ID (optional)<br><input id="tmn_device_id" placeholder="" /></label>
      <label>TMN_PIN6<br><input id="tmn_pin6" type="password" placeholder="123456" /></label>

      <label>PROXY_IP (optional)<br><input id="tmn_proxy_ip" placeholder="" /></label>
      <label>PROXY_USERNAME (optional)<br><input id="tmn_proxy_user" placeholder="" /></label>

      <label>PROXY_PASSWORD (optional)<br><input id="tmn_proxy_pass" type="password" placeholder="" /></label>
    </div>

    <div style="margin-top:10px;display:flex;gap:8px;flex-wrap:wrap;align-items:center;">
      <button class="btn-primary" id="tmn_save" type="button">Save TMN</button>
      <button id="tmn_clear" type="button">Clear TMN</button>
      <span id="tmnStatus" class="muted"></span>
    </div>
  </div>

  <div class="card">
    <div class="row" style="align-items:flex-end;">
      <div>
        <div class="muted">ADMIN KEY</div>
        <input id="adminKey" placeholder="mmk1000" style="width:220px" />
      </div>
      <div style="display:flex;gap:8px;flex-wrap:wrap;">
        <button class="btn-primary" type="button" onclick="saveAdminKey()">Save</button>
        <button type="button" onclick="authCheck()">Auth Check</button>
        <button type="button" onclick="pingHealth()">Ping /api/health</button>
        <button id="debugBtn" type="button" onclick="toggleDebug()">DEBUG: OFF</button>
        <span id="status" class="muted"></span>
      </div>
    </div>
  </div>

  <div id="tab-dashboard" class="section active">
    <div class="row">
      <div class="card">
        <h3>1) Dashboard</h3>

        <div class="row" style="align-items:flex-end;">
          <div>
            <div class="muted">Start</div>
            <input id="start" type="date" />
          </div>
          <div>
            <div class="muted">End</div>
            <input id="end" type="date" />
          </div>
          <div>
            <button class="btn-primary" type="button" onclick="loadDashboard()">Refresh</button>
          </div>
        </div>

        <div style="margin:10px 0 6px;">
          <div class="muted">Account</div>
          <div><b id="accountName">-</b></div>
        </div>

        <div class="row" style="margin:10px 0;">
          <div class="card" style="min-width:220px;">
            <div class="muted">Balance</div>
            <div><b id="balance">-</b> <span class="muted" id="balMode"></span></div>
          </div>
          <div class="card" style="min-width:220px;">
            <div class="muted">In (sum)</div>
            <div><b id="sumIn">-</b></div>
          </div>
          <div class="card" style="min-width:220px;">
            <div class="muted">Out (sum)</div>
            <div><b id="sumOut">-</b></div>
          </div>
        </div>

        <div class="table-wrap">
          <table>
            <thead><tr><th>time</th><th>type</th><th>amount</th><th>id</th></tr></thead>
            <tbody id="txBody"></tbody>
          </table>
        </div>

        <div class="muted" style="margin-top:10px;">* ถ้า backend ส่งชื่อบัญชีมา (เช่น account_name) หน้าจะโชว์ให้เอง</div>
      </div>
    </div>
  </div>

  <div id="tab-withdraw" class="section">
    <div class="card">
      <h3>2) Withdraw (คิว + อนุมัติ)</h3>

      <!-- row 1 -->
      <div class="row" style="align-items:flex-end;">
        <div>
          <div class="muted">Type</div>
          <select id="wType" onchange="renderWithdrawFields()">
            <option value="bank">Bank</option>
            <option value="promptpay">PromptPay</option>
            <option value="wallet">TrueWallet (P2P)</option>
          </select>
        </div>
        <div>
          <div class="muted">Amount</div>
          <input id="wAmount" type="number" step="0.01" inputmode="decimal" placeholder="0.00" />
        </div>
        <div style="display:flex;gap:8px;flex-wrap:wrap;">
          <button class="btn-primary" type="button" onclick="createWithdraw()">Add Queue</button>
          <button type="button" onclick="loadQueue()">Refresh Queue</button>
        </div>
      </div>

      <!-- row 2: dest fields -->
      <div class="row" style="margin-top:10px;">
        <div class="card" style="flex:1; min-width:280px;">
          <div class="muted">Destination</div>
          <div id="wFields" style="margin-top:8px;"></div>
          <div class="muted" id="destHint" style="margin-top:8px;"></div>
        </div>

        <!-- ✅ QR อยู่หน้าเดียวกับ Withdraw -->
        <div class="card" style="flex:1; min-width:280px;">
          <div class="muted">QR PromptPay (decode → เติม proxy)</div>
          <div class="row" style="align-items:flex-end; margin-top:8px;">
            <div style="flex:1;">
              <input id="qrFile" type="file" accept="image/*" />
            </div>
            <div>
              <button class="btn-primary" type="button" onclick="decodeQrIntoWithdraw()">Decode</button>
            </div>
          </div>
          <pre id="qrOut" style="white-space:pre-wrap; margin-top:10px;"></pre>
        </div>
      </div>

      <div id="qCards" style="margin-top:12px;"></div>

      <div class="table-wrap" id="qTable" style="margin-top:12px;">
        <table>
          <thead><tr><th>status</th><th>type</th><th>amount</th><th>dest</th><th>actions</th></tr></thead>
          <tbody id="qBody"></tbody>
        </table>
      </div>

      <div class="muted" style="margin-top:10px;">* Approve = เขียว, Send = แดง และมี confirm รายละเอียดก่อนทำงาน</div>
    </div>
  </div>

  <script src="/app.js?v=20260209-3"></script>
</body>
</html>
